// Layouts will serve as the Layouts for pages if there is any need
// for that
// layouts are what they sound like they are, they serve as a helper that can help
// to configure the looks of the a component. Likely to be resusable
